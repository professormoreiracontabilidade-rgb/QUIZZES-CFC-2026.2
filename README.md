# QUIZZES CFC 2026.2

Envie todos os arquivos para a raiz do repositório e ative o GitHub Pages em Settings > Pages > Deploy from a branch > main > /root.

Para acrescentar quizzes, edite apenas `data/quizzes.json`. A quantidade de módulos é calculada automaticamente. Em cada atualização, altere o nome do cache em `service-worker.js`.
